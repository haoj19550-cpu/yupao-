# 1.前端项目初始化

## 使用脚手架初始化项目：

-   使用vite构建：yarn create vite

![image-20260310143837558](C:\Users\27843\AppData\Roaming\Typora\typora-user-images\image-20260310143837558.png)

选择vue，脚本选择ts

## 组件库：

- 安装vant：

npm i vant

vant:https://vant-ui.github.io/vant/#/zh-CN/quickstart

- 使用按需 引入，简化项目体积：

-  `npm i @vant/auto-import-resolver unplugin-vue-components unplugin-auto-import -D`

# 2.前端主页+组件概览

导航条：展示当前页面名称

主页搜素框 => 搜索页=>搜索结果页

内容

tab栏：

-  主页（推荐页+**广告**）

1. 搜索框
2. banner
3. 推荐信息流

- 队伍页
- 用户页（消息 - 发邮件）

# 开发后端接口

## 搜索标签

  1.允许用户传入多个标签，多个标签存在才搜索出来and。 like '%java%' and like '%c++%'

  2.允许用户传入多个标签，多个标签存在才搜索出来or。 like '%java%' or like '%c++%'

查询方式：

1.SQL查询（实现简单）

```
QueryWrapper<User> queryWrapper = new QueryWrapper<>();
for (String tagName : tagNameList){
    queryWrapper = queryWrapper.like("tags",tagName);
}
List<User> userList = userMapper.selectList(queryWrapper);
```

2.内存查询：（灵活）

**查询优化**：

- 如果参数可以分析，根据用户的参数去选择查询方法，比如标签数
- 如果参数不可以分析，并且数据库连接足够、内存空间足够，可以并发同时查询，谁先返回用谁
- 还可以sql查询与内存查询相结合



   数据表有json格式字段时，需要将json转成java对象（反序列化）

   将java对象转成json(序列化)

   java json序列化库有很多：

​       1.gson（google）

​       2.fastjson(ali 虽然快，但是漏洞多)

​       3.jackson

​      4.kryo

![image-20260311213552121](C:\Users\27843\AppData\Roaming\Typora\typora-user-images\image-20260311213552121.png)

## swagger接口生成

swagger已经不维护了，用**OpenAPI**方案

https://blog.csdn.net/2301_79989083/article/details/155708318

## 存量用户信息导入及同步（使用爬虫爬信息，导入）

## 流程

1.    从excel中导入全量用户数据，判重 easy excel:https://easyexcel.opensource.alibaba.com/docs/current/
2.    抓取写了自我介绍的同学信息，提取用户昵称、用户唯一ID、自我介绍信息
3.   从自我介绍中提取信息，然后写入到数据库中

## EasyExcel

两种读方式

   1.确定表头：建立对象,和表头形成映射

  ![image-20260314204926554](C:\Users\27843\AppData\Roaming\Typora\typora-user-images\image-20260314204926554.png)

​        **建立监听器**

![image-20260314212041223](C:\Users\27843\AppData\Roaming\Typora\typora-user-images\image-20260314212041223.png)

   **读excel的方法![image-20260314212241453](C:\Users\27843\AppData\Roaming\Typora\typora-user-images\image-20260314212241453.png)**



   2.不确定表头：每一行数据映射为Map<String,Object>

## 往数据库导入数据

   1.用可视化界面：适合一次性导入，但是得数据量可控

   2.写程序：for循环，分批插入，不要全部一起插入：**要保证可控、幂等，**

​       注意线上环境和测试环境是有区别得

   3.执行sql语句：使用于小数据量

## 插入数据库时编写一次性操作

for循环插入数据的问题：

   1.建立和释放数据库连接（批量查询解决）

![image-20260317223725499](C:\Users\27843\AppData\Roaming\Typora\typora-user-images\image-20260317223725499.png)

2.for循环是绝对线性的（使用并发）

   **并发要注意执行的先后顺序是否无所谓，不要用到非并发类的集合**

![image-20260317223828227](C:\Users\27843\AppData\Roaming\Typora\typora-user-images\image-20260317223828227.png)

![image-20260317223838882](C:\Users\27843\AppData\Roaming\Typora\typora-user-images\image-20260317223838882.png)

## 推荐算法

大型app两种方式：

- 实时推荐：用户进入页面时，实时计算
- 离线推荐：提前进行推荐

## 数据查询慢怎么办？

用缓存：提前把数据存起来（通常保存到读写更快的介质，比如内存），可以更快的读写

### 缓存的实现

- Redis（分布式缓存）
- memcached(分布式)

------

- ehcache(单机)
- 本地缓存（java内存Map）
- Caffeine(Java内存缓存，高性能)
- Goole Guava

# Redis

key - value 存储系统 （区别于MySql，他存储的是键值对）

## Redis数据结构

String 字符串：name: "yupi"

List列表：names:["yupi","cat","cat"]

Set集合：names:["yupi","cat","dog"] (不允许重复)

Zset集合：names:{yupi - 9,dogyupi - 12}（适合做排行榜）

Hash 哈希：nameAge：{"yupi":1,"dog":2}

## 设计缓存key

不同用户看到的数据不同

systemId:moduleId:func:options(这样设置key的话，不容易和别人冲突，保证唯一性)

yupao:user:recommed:userId

**redis内存不能无限增加，一定要设置过期时间，不然会造成内存泄漏**

![image-20260321111038843](C:\Users\27843\AppData\Roaming\Typora\typora-user-images\image-20260321111038843.png)

## 缓存预热

问题：第一个用户访问慢，该怎么解决

- 优点：**缓存预热**是指在系统启动或高峰期来临之前，提前将热点数据加载到 Redis 等缓存中，避免在请求到来时因缓存缺失而直接穿透到数据库，导致数据库压力骤增、响应变慢甚至崩溃。其核心目标是**“让缓存提前就位，平滑承接流量”**。

​       缓存预热是保障高并发系统平稳运行的常见手段，本质是**“用空间换时间，用提前准备换实时压力”**。在项目中，需要根据业务特点      （热点数据可预知程度、周期性、数据量等）选择合适的预热时机与实现方式，并配合监控持续优化。

- 缺点：

  1.增加开发成本（需要额外的开发、设计）

  2.预热的时机和时间如果错了，有可能缓存的数据不对，或者过时了

### 怎么实现缓存预热？

   1.定时

​      **实现**

​       用定时任务，每天刷新所有用户的推荐列表

​     注意点：

​       （1）缓存预热的意义是新增少，但是总用户多

​       （2）缓存的空间不能太大，要预留给其他缓存空间

​       （3）缓存数据的周期（此项目每天一次）

#### 控制定时任务的执行

如果有100台服务器，难道这个任务在这些服务器上都执行吗？

这样会浪费很大资源，如果有脏数据（重复插入），更加严重

**所以要控制定时任务在同一时间只有一个服务器能执行**

方法：

- 分离定时任务和主程序，只在一个服务器运行定时任务，但是成本太大

- 写死配置，每个服务器都执行这个定时任务，前面加一个if判断，只有符合ip地址的服务器才可以真正执行任务。成本最低，但是ip地址不固定，不能把ip地址写死

  ```java
  if(ip == 服务器地址){return true}
  ```

-   动态配置，配置是可以轻松的、很方便地更新的（**代码无需重启**），但是只有ip符合配置的服务器才真实执行任务
  - 数据库
  - Redis
  - 配置中心（Nacos、Apollo、Spring Cloud Config)

​         问题：服务器多了、IP不可控，很麻烦，依然需要人工修改 

- 分布式锁，只有抢到锁的才可以执行任务

  - **锁**

    在有限资源的情况下，控制同一时间只有某些线程（用户/服务器）能访问到资源。

    Java实现锁：synchornized关键字、并发包的类

    问题：只对单个JVM有效，也就是只能在写了锁的服务器生效

​          **为啥需要分布式锁**

​            1.在有限资源的情况下，控制同一时间只有某些线程（用户/服务器）能访问到资源

​            2.单个锁只对单个JVM有效

2.模拟触发（手动触发）

# 分布式锁实现的关键

## 抢锁机制

怎么保证同一时间只有一个服务器能抢到锁？

**核心思想**：先进来的人先把数据改成自己的标识（服务器ip），后来的人发现标识已存在，就抢锁失败，继续等待。

等先来的人执行方法结束，把标识清空，其他的人继续抢锁。



Mysql数据库：select for update 行级锁（最简单）

（乐观锁）

Redis实现：内存数据库，读写速度快。支持setnx、lua脚本，比较方便实现分布式锁

setnx：set if not exists 如果不存在，则设置；只有设置成功才会返回true，否则返回false

## 注意事项

   1.用完锁要释放

   2.锁一定要加过期时间（如果redis崩了，但是锁的标识还在，就会让其他服务器抢不到锁）

   3.如果方法执行时间过长，锁提前过期了？（解决方法：看门狗自动续期）

​       问题：

​          1.连锁效应：释放掉别人的锁

**解决方法**

          ```java
           if (lock.isHeldByCurrentThread()){
                          lock.unlock();
                      }
          ```



​          2.这样还是会存在多个方法同时执行的情况

## 定时任务+锁

​    1.waitTIme设置为0，只抢一次，抢不到就放弃

​    2.注意释放锁要写在finaly中



## 看门狗机制

> redisson中提供的续期机制

开一个监听线程，如果任务还在执行，就重置过期时间，**需要注意的是等待时间设为-1，如果设置了等待时间，则不会自动续期**

![image-20260329171050741](C:\Users\27843\AppData\Roaming\Typora\typora-user-images\image-20260329171050741.png)

# 编辑距离算法

> 本质：找到有相似标签的用户

   1.找到有共同标签最多的用户（TOPN）

   2.共同标签越多，分数越高，越排在前面

   3.如果没有匹配的用户，随机推荐几个（降级方案）

编辑距离算法：字符串1通过多少次增删改操作可以变成字符串2

余弦相似度算法：

# 前端开发页面

- 用户页面跳转到对应的修改页面

  ​    在修改页面要返回用户页面时，点击返回按钮，却回到主页？

  ![image-20260314161017916](C:\Users\27843\AppData\Roaming\Typora\typora-user-images\image-20260314161017916.png)

- 点击标签栏中的标签时，中上方的标题随着改变？


- 使用axios向后端发送请求时，请求的键值对的key可以变化

  ![image-20260317201409466](C:\Users\27843\AppData\Roaming\Typora\typora-user-images\image-20260317201409466.png)

# 开发时的问题

![image-20260310170128253](C:\Users\27843\AppData\Roaming\Typora\typora-user-images\image-20260310170128253.png)

如果引入文件时，地址报红，解决方法：

   ![image-20260310170238469](C:\Users\27843\AppData\Roaming\Typora\typora-user-images\image-20260310170238469.png)

在该目录下创建这个文件，文件里的内容：

```
/// <reference types="vite/client" />

declare module "*.vue" {
  import type { DefineComponent } from "vue";
  const component: DefineComponent<{}, {}, any>;
  export default component;
}
```

# 
