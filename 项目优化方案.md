# 伙伴匹配系统 — 全栈项目优化方案

## 一、项目现状总结

| 维度 | 现状 |
|------|------|
| 后端框架 | Spring Boot 2.6.4 + MyBatis-Plus 3.5.1 + Java 8 |
| 前端框架 | Vue 3.2 + Vite 2.9 + Vant 3.4 + TypeScript 4.5 |
| 数据层 | MySQL 8 + Redis + Redisson 3.17.5 |
| 核心功能 | 用户注册/登录、标签搜索、相似匹配、组队 CRUD、缓存预热 |

---

## 二、安全性优化（优先级：高）

### 2.1 密码加密算法升级

**问题**：`UserServiceImpl.java:92` 使用 `MD5 + 固定盐值 "yupi"` 加密密码，MD5 已被证明不安全，固定盐值易被彩虹表破解。

**优化方案**：替换为 BCrypt 自适应哈希算法。

```java
// 旧代码
private static final String SALT = "yupi";
String encryptPassword = DigestUtils.md5DigestAsHex((SALT + userPassword).getBytes());

// 新代码
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
private static final BCryptPasswordEncoder passwordEncoder = new BCryptPasswordEncoder();
String encryptPassword = passwordEncoder.encode(userPassword);
// 验证时：passwordEncoder.matches(rawPassword, encodedPassword)
```

**预期效果**：抗彩虹表攻击能力提升数个数量级，BCrypt 自带随机盐且计算成本可调。

### 2.2 登录接口防暴力破解  ✅️

**问题**：`/user/login` 无任何频率限制，攻击者可无限次尝试密码。

**优化方案**：基于 Redis 实现登录限流。

```java
String limitKey = String.format("login:limit:%s", userAccount);
Long count = redisTemplate.opsForValue().increment(limitKey);
if (count != null && count == 1) {
    redisTemplate.expire(limitKey, 5, TimeUnit.MINUTES);
}
if (count != null && count > 5) {
    throw new BusinessException(ErrorCode.FORBIDDEN, "登录尝试过多，请5分钟后再试");
}
```

**预期效果**：5 分钟内同一账号最多尝试 5 次登录，有效防止暴力破解。

### 2.3 跨域配置收紧 ✅️

**问题**：`WebMvcConfg.java:27` 使用 `allowedMethods("*")` 放行所有 HTTP 方法。

**优化方案**：

```java
.allowedMethods("GET", "POST", "PUT", "DELETE")
```

同时将 `allowedOrigins` 改为从配置文件读取，避免硬编码域名。

**预期效果**：减少不必要的 HTTP 方法暴露，降低 CSRF 等攻击面。

### 2.4 队伍密码明文存储❌️

**问题**：`TeamServiceImpl.java:254` 队伍密码明文比较 `password.equals(team.getPassword())`，数据库中 `team.password` 也是明文。

**优化方案**：队伍密码同样使用 BCrypt 加密存储，验证时使用 `matches()`。

**预期效果**：避免数据库泄露后队伍密码直接暴露。

### 2.5 用户 ID 比较使用 `==` 而非 `equals`

**问题**：`TeamServiceImpl.java:222,331,364` 中 `oldTeam.getUserId() != loginUser.getId()` 使用 `!=` 比较 `Long` 对象，超过 127 时会因缓存失效而比较错误。

**优化方案**：全部改为 `.equals()` 或先拆箱为 `long` 基本类型：

```java
if (!Objects.equals(oldTeam.getUserId(), loginUser.getId()) ...
```

**预期效果**：修复潜在的用户 ID 比较错误 Bug，该问题在用户量增长后必然触发。

### 2.6 `TeamAddRequest` 暴露了 `userId` 字段

**问题**：`TeamAddRequest.java:42` 包含 `userId` 字段，允许前端传入任意 userId，存在越权风险。

**优化方案**：移除 `userId`，队伍的创建者应从 `loginUser` 中获取。

**预期效果**：消除越权创建队伍的安全漏洞。

---

## 三、性能优化（优先级：高）

### 3.1 匹配算法 N+1 查询 + 全表扫描

**问题**：`UserServiceImpl.java:271-316` 的 `matchUsers()` 方法：
1. 查询**所有用户**到内存（`this.list(queryWrapper)`）
2. 计算所有用户的编辑距离（O(n×m) 时间复杂度）
3. 再根据 topN 的 ID 列表**二次查询**用户信息

**优化方案**：

**第一步：空间优化 — 编辑距离使用滚动数组**

```java
public static int minDistance(List<String> tagList1, List<String> tagList2) {
    int n = tagList1.size(), m = tagList2.size();
    int[] prev = new int[m + 1];
    int[] curr = new int[m + 1];
    for (int j = 0; j <= m; j++) prev[j] = j;
    for (int i = 1; i <= n; i++) {
        curr[0] = i;
        for (int j = 1; j <= m; j++) {
            int left = curr[j - 1] + 1;
            int down = prev[j] + 1;
            int diag = prev[j - 1];
            if (!Objects.equals(tagList1.get(i - 1), tagList2.get(j - 1))) diag++;
            curr[j] = Math.min(left, Math.min(down, diag));
        }
        int[] temp = prev; prev = curr; curr = temp;
    }
    return prev[m];
}
```

空间复杂度从 O(n×m) 降至 O(min(n,m))。

**第二步：使用优先队列（小顶堆）替代全排序**

```java
PriorityQueue<Pair<User, Long>> topQueue = new PriorityQueue<>(
    Comparator.comparingLong(Pair::getValue)
);
for (User user : userList) {
    long distance = AlgorithmUtils.minDistance(tagList, userTagList);
    topQueue.add(new Pair<>(user, distance));
    if (topQueue.size() > num) topQueue.poll(); // 保持堆大小为 num
}
```

排序复杂度从 O(n log n) 降至 O(n log k)，k 为需要的匹配数量。

**第三步：异步/批量查询替代 N+1**

将二次查询合并为一次 IN 查询（已有），但应使用 `selectBatchIds` 替代手动拼接 `QueryWrapper`。

**预期效果**：内存占用降低 90%+，匹配计算速度提升，排序效率从 O(n log n) 降至 O(n log k)。

### 3.2 标签搜索全量加载到内存

**问题**：`UserServiceImpl.java:190-211` 的 `searchUsersByTags()` 将**全表用户**加载到内存再过滤，随用户量增长 OOM 风险极高。

**优化方案**：恢复使用 SQL LIKE 查询（原 `searchUsersByTagsBySQL` 方法），或使用 MySQL JSON 函数：

```java
queryWrapper.apply("JSON_CONTAINS(tags, {0})", 
    gson.toJson(tagNameList.get(0)));
```

对于大数据量场景，可引入 Elasticsearch 做标签索引。

**预期效果**：消除 OOM 风险，查询效率从全表扫描提升至索引查询。

### 3.3 队伍列表 N+1 查询

**问题**：`TeamServiceImpl.java:189-204` 的 `listTeams()` 在 for 循环中逐个查询创建人信息：

```java
for (Team team : teamList) {
    User user = userService.getById(userId);  // N+1 问题
}
```

**优化方案**：批量查询用户：

```java
Set<Long> userIdSet = teamList.stream()
    .map(Team::getUserId).filter(Objects::nonNull).collect(Collectors.toSet());
Map<Long, User> userMap = userService.listByIds(userIdSet).stream()
    .collect(Collectors.toMap(User::getId, Function.identity()));
// 然后在循环中从 map 取值
```

**预期效果**：数据库查询次数从 N+1 降至 2，接口响应时间大幅缩短。

### 3.4 缓存 TTL 过短

**问题**：`UserController.java:138` 推荐缓存 TTL 仅 **30秒**（`30000, TimeUnit.MILLISECONDS`），`PreCacheJob.java:60` 同样。频繁失效导致缓存命中率极低。

**优化方案**：
- 推荐列表 TTL 设为 5-10 分钟
- 用户更新信息时主动删除缓存（Cache-Aside 模式）
- 定时任务预热时设为 30 分钟

**预期效果**：缓存命中率从极低提升至 80%+，数据库压力显著降低。

### 3.5 RedisTemplate 未设置 Value 序列化器

**问题**：`RedisTemplateConfig.java:24` 只设置了 Key 的序列化器，Value 使用默认的 JDK 序列化，导致：
1. 存储体积大
2. 跨服务反序列化易失败
3. 可读性差

**优化方案**：

```java
redisTemplate.setKeySerializer(RedisSerializer.string());
redisTemplate.setValueSerializer(RedisSerializer.json());
redisTemplate.setHashKeySerializer(RedisSerializer.string());
redisTemplate.setHashValueSerializer(RedisSerializer.json());
```

**预期效果**：Redis 存储体积减少 50%+，数据可读性提升，跨服务兼容性增强。

---

## 四、代码质量优化（优先级：中）

### 4.1 `getSafetyUser` 手动字段拷贝 — 使用 BeanUtils

**问题**：`UserServiceImpl.java:151-169` 手动逐字段拷贝 13 个属性，新增字段易遗漏。

**优化方案**：

```java
public User getSafetyUser(User originUser) {
    if (originUser == null) return null;
    User safetyUser = new User();
    BeanUtils.copyProperties(originUser, safetyUser);
    safetyUser.setUserPassword(null); // 清除敏感字段
    return safetyUser;
}
```

**预期效果**：新增字段时无需手动维护拷贝逻辑，减少遗漏风险。

### 4.2 返回值不一致 — 注册失败返回 null vs 异常

**问题**：`UserController.java:56` 注册校验失败返回 `null`，而其他接口抛出 `BusinessException`。登录失败也返回 `null` 而非统一异常。

**优化方案**：统一使用 `BusinessException`：

```java
if (StringUtils.isAnyBlank(userAccount, userPassword, checkPassword, planetCode)) {
    throw new BusinessException(ErrorCode.PARAMS_ERROR);
}
```

**预期效果**：异常处理风格统一，前端无需针对不同接口做特殊处理。

### 4.3 `TeamController.listTeams` 空异常吞没

**问题**：`TeamController.java:113` 空 catch 块吞掉了获取登录用户时的异常：

```java
try {
    User loginUser = userService.getLoginUser(request);
    ...
} catch (Exception e) {
    // 空 catch，完全忽略异常
}
```

**优化方案**：至少记录日志，或正确处理未登录状态：

```java
} catch (BusinessException e) {
    log.info("用户未登录，不标记已加入状态");
}
```

**预期效果**：异常不再被静默吞没，便于排查线上问题。

### 4.4 `System.out.println` 替换为日志

**问题**：`PreCacheJob.java:52,71` 和 `TeamServiceImpl.java:266,300` 使用 `System.out.println` 输出调试信息。

**优化方案**：全部替换为 `log.debug()`，类上已有 `@Slf4j` 注解。

**预期效果**：日志可控（级别、输出位置、格式），生产环境可关闭 debug 输出。

### 4.5 重复的 `serialVersionUID`

**问题**：多个 Request 类共用相同的 `serialVersionUID = 3191241716373120793L`，应各自唯一。

**优化方案**：为每个类生成唯一的 serialVersionUID，或使用 IDE 自动生成。

**预期效果**：避免序列化版本冲突。

### 4.6 `@EnableRedisHttpSession` 注解缺失

**问题**：`MyApplication.java` 缺少 `@EnableRedisHttpSession` 注解，但 `application.yml` 配置了 `spring.session.store-type=redis`，Spring Boot 自动配置可能生效，但显式声明更明确可靠。

**优化方案**：在 `MyApplication.java` 上添加 `@EnableRedisHttpSession`。

**预期效果**：配置意图更清晰，避免自动配置失效的隐患。

---

## 五、前端优化（优先级：中）

### 5.1 Vant 全量导入

**问题**：`main.ts:5-6` 全量导入 Vant 组件库，导致打包体积膨胀（dist/js 约 329KB）。

**优化方案**：改用按需导入：

```ts
import { NavBar, Tabbar, Card, Button, Form, Field } from 'vant';
app.use(NavBar).use(Tabbar).use(Card)...
```

或配合 `unplugin-vue-components` 自动按需导入。

**预期效果**：打包体积减少 50%+，首屏加载速度提升。

### 5.2 路由无懒加载

**问题**：`route.ts` 所有页面使用静态 import，首屏加载全部组件。

**优化方案**：

```ts
const routes = [
  { path: '/', component: () => import('../pages/Index.vue') },
  { path: '/team', component: () => import('../pages/TeamPage.vue') },
  // ...
]
```

**预期效果**：首屏 JS 体积大幅减少，页面加载速度提升。

### 5.3 路由无权限守卫

**问题**：无 `router.beforeEach` 鉴权，用户可直接访问 `/user/update` 等需登录页面。

**优化方案**：添加路由守卫：

```ts
router.beforeEach(async (to) => {
  const publicPages = ['/user/login'];
  if (!publicPages.includes(to.path)) {
    const user = await getCurrentUser();
    if (!user) return '/user/login';
  }
});
```

**预期效果**：未登录用户无法直接访问需要登录的页面。

### 5.4 全局状态管理简陋

**问题**：`states/user.ts` 使用模块级变量存储用户状态，页面刷新即丢失，且非响应式。

**优化方案**：引入 Pinia 状态管理：

```ts
export const useUserStore = defineStore('user', {
  state: () => ({ currentUser: null as UserType | null }),
  actions: {
    async fetchUser() { this.currentUser = await getCurrentUser(); }
  }
});
```

**预期效果**：状态响应式更新，支持持久化，组件间共享更优雅。

### 5.5 Axios 响应拦截器问题

**问题**：`myAxios.ts:28` 直接返回 `response.data`，但 `Index.vue:39` 又访问 `response?.data`，导致数据访问层级混乱。

**优化方案**：统一处理策略，建议拦截器只做 401 跳转，数据解包在业务层进行。

**预期效果**：数据访问层级一致，减少 undefined 错误。

### 5.6 标签数据硬编码

**问题**：`SearchPage.vue:39-57` 标签列表硬编码在前端，如 `{text: '大5aaaaaaa', id: '大5aaaaaaa'}` 明显是测试数据。

**优化方案**：标签数据应从后端接口获取，或至少抽为配置文件。

**预期效果**：标签数据可动态维护，消除测试数据残留。

### 5.7 `UserCardList.vue` 引用不存在的字段

**问题**：`:desc="user.profile"` 但 `UserType` 中无 `profile` 字段，始终为 undefined。

**优化方案**：检查并修正字段名，或补全 `UserType` 定义。

**预期效果**：用户卡片描述信息正常显示。

### 5.8 `dist/` 目录不应提交到 Git

**问题**：`.gitignore` 已忽略 `dist`，但仓库中实际存在 `dist/` 目录。

**优化方案**：执行 `git rm -r --cached dist/` 并提交。

**预期效果**：仓库体积减小，避免构建产物污染版本控制。

---

## 六、可维护性与扩展性优化（优先级：中低）

### 6.1 引入参数校验框架

**问题**：大量手动 if-else 校验，如 `UserServiceImpl.java:53-93` 的注册校验。

**优化方案**：引入 `spring-boot-starter-validation` + `@Valid`：

```java
public BaseResponse<Long> userRegister(@Valid @RequestBody UserRegisterRequest req) {
    // Request 类上使用注解：@NotBlank @Size(min=4, max=256) 等
}
```

**预期效果**：校验逻辑从业务代码中解耦，代码量减少 50%+。

### 6.2 引入统一权限拦截器

**问题**：每个接口手动调用 `userService.isAdmin(request)` 或 `userService.getLoginUser(request)`，重复代码多。

**优化方案**：实现 `HandlerInterceptor` + 自定义注解 `@RequireLogin` / `@RequireAdmin`，在拦截器中统一处理。

**预期效果**：权限校验逻辑集中管理，Controller 代码更简洁。

### 6.3 接口返回脱敏用户 VO 而非原始 User

**问题**：`UserController.java:109` 的 `searchUsers` 返回 `List<User>`，虽然做了 `getSafetyUser` 处理，但应返回 `UserVO` 以明确语义。

**优化方案**：创建 `UserVO` 类，Controller 返回 `List<UserVO>`。

**预期效果**：接口语义更清晰，前后端契约更明确。

### 6.4 数据库索引优化

**问题**：`create_table.sql` 中 `user` 表缺少 `userAccount`（唯一索引）、`planetCode`（唯一索引）的索引，注册查重时全表扫描。

**优化方案**：

```sql
ALTER TABLE user ADD UNIQUE INDEX uk_userAccount (userAccount);
ALTER TABLE user ADD UNIQUE INDEX uk_planetCode (planetCode);
ALTER TABLE user_team ADD INDEX idx_teamId (teamId);
ALTER TABLE user_team ADD INDEX idx_userId (userId);
```

**预期效果**：注册查重和关联查询效率提升 10 倍+。

### 6.5 版本升级

| 组件 | 当前版本 | 建议版本 | 原因 |
|------|---------|---------|------|
| Spring Boot | 2.6.4 | 3.x | 安全补丁 + 性能提升 |
| Vue | 3.2.25 | 3.4+ | 性能优化 + 更好的 TS 支持 |
| Vite | 2.9.5 | 5.x | 构建速度大幅提升 |
| Axios | 0.27.2 | 1.x | CSRF 修复 + TS 类型改进 |
| Gson | 2.8.9 | Jackson | Spring Boot 内置，减少依赖 |

**预期效果**：获取最新安全补丁、性能优化和更好的开发体验。

---

## 七、实施优先级路线图

| 阶段 | 优化项 | 预期效果 |
|------|--------|---------|
| **P0 立即修复** | MD5→BCrypt、Long==→equals、登录限流、空catch修复、TeamAddRequest越权 | 消除安全漏洞和潜在Bug |
| **P1 短期优化** | N+1查询修复、缓存TTL调整、Redis序列化器、Vant按需导入 | 性能提升 5-10 倍，包体积减半 |
| **P2 中期优化** | 编辑距离空间优化、路由懒加载/守卫、Pinia状态管理、参数校验框架 | 可维护性显著提升 |
| **P3 长期规划** | ES标签搜索、协同过滤推荐、WebSocket消息、Spring Boot 3升级 | 支撑业务规模增长 |

> 每项优化均可独立实施，建议按优先级逐步推进，每完成一个阶段进行回归测试确认无回归问题。
